name: 🔄 Sync TIL to Portfolio

on:
  push:
    branches: [main]
    paths: ["**/*.md"]
  workflow_dispatch:

jobs:
  sync-til:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout TIL repo
        uses: actions/checkout@v3
        with:
          path: til-repo

      - name: Checkout Portfolio repo
        uses: actions/checkout@v3
        with:
          repository: MinHyeok-lee1/MinHyeok-lee1-portfolio
          token: ${{ secrets.PERSONAL_GITHUB_TOKEN }}
          path: portfolio-repo

      - name: Cleanup old TIL pages
        run: |
          rm -rf portfolio-repo/src/pages/til

      - name: Sync and convert TIL files
        run: |
          set -e
          SRC_BASE="til-repo"
          DEST_BASE="portfolio-repo/src/pages/til"

          mkdir -p "$DEST_BASE"

          find "$SRC_BASE" -type f -name "*.md" ! -path "$SRC_BASE/tags/*" | grep -vE '^til-repo/README\.md$' | while read FILE; do
            REL_PATH="${FILE#$SRC_BASE/}"
            YEAR=$(echo "$REL_PATH" | cut -d'/' -f1)
            MONTH=$(echo "$REL_PATH" | cut -d'/' -f2)
            FNAME=$(basename "$REL_PATH" .md)

            [[ "$YEAR" =~ ^[0-9]{4}$ ]] || continue
            [[ "$MONTH" =~ ^[0-9]{2}$ ]] || continue

            DEST_DIR="$DEST_BASE/til-$YEAR/til-$MONTH"
            mkdir -p "$DEST_DIR"
            cp "$FILE" "$DEST_DIR/$FNAME.mdx"
          done

          TAGS_SRC="$SRC_BASE/tags"
          TAGS_DEST="$DEST_BASE/tags"
          mkdir -p "$TAGS_DEST"

          shopt -s nullglob
          for FILE in "$TAGS_SRC"/*.md; do
            NAME=$(basename "$FILE" .md)
            cp "$FILE" "$TAGS_DEST/$NAME.mdx"
          done
          shopt -u nullglob

      - name: Create tags/_meta.json and index.mdx
        run: |
          TAGS_DIR="portfolio-repo/src/pages/til/tags"
          mkdir -p "$TAGS_DIR"

          echo '{' > "$TAGS_DIR/_meta.json"
          echo '  "index": "소개"' >> "$TAGS_DIR/_meta.json"
          FIRST=1
          for FILE in "$TAGS_DIR"/*.mdx; do
            NAME=$(basename "$FILE" .mdx)
            echo '  ,"'$NAME'": "'$NAME' 태그"' >> "$TAGS_DIR/_meta.json"
          done
          echo "}" >> "$TAGS_DIR/_meta.json"

          echo "# 🏷️ 태그 모음" > "$TAGS_DIR/index.mdx"
          echo "| 태그 | Nextra 바로가기 | GitHub 바로가기 |" >> "$TAGS_DIR/index.mdx"
          echo "|------|------------------|------------------|" >> "$TAGS_DIR/index.mdx"

          for FILE in "$TAGS_DIR"/*.mdx; do
            NAME=$(basename "$FILE" .mdx)
            echo "| $NAME | [Nextra 바로가기](/til/tags/$NAME) | [GitHub 바로가기](https://github.com/MinHyeok-lee1/TIL/blob/main/tags/$NAME.md) |" >> "$TAGS_DIR/index.mdx"
          done

      - name: Create all _meta.json and index.mdx files
        run: |
          BASE="portfolio-repo/src/pages/til"

          # til/_meta.json
          echo '{' > "$BASE/_meta.json"
          echo '  "index": "소개",' >> "$BASE/_meta.json"
          echo '  "tags": "태그모음"' >> "$BASE/_meta.json"

          # tags/_meta.json, tags/index.mdx
          TAGS_DIR="$BASE/tags"
          mkdir -p "$TAGS_DIR"
          echo '{' > "$TAGS_DIR/_meta.json"
          echo '  "index": "소개"' >> "$TAGS_DIR/_meta.json"
          FIRST=1
          FILES=( "$TAGS_DIR"/*.mdx )
          for FILE in "${FILES[@]}"; do
            [ -f "$FILE" ] || continue
            NAME=$(basename "$FILE" .mdx)
            if [ $FIRST -eq 1 ]; then
              echo '  ,"'$NAME'": "'$NAME' 태그"' >> "$TAGS_DIR/_meta.json"
              FIRST=0
            else
              echo '  ,"'$NAME'": "'$NAME' 태그"' >> "$TAGS_DIR/_meta.json"
            fi
          done
          echo "}" >> "$TAGS_DIR/_meta.json"

          echo "# 🏷️ 태그 모음" > "$TAGS_DIR/index.mdx"
          echo "| 태그 | Nextra 바로가기 | GitHub 바로가기 |" >> "$TAGS_DIR/index.mdx"
          echo "|------|------------------|------------------|" >> "$TAGS_DIR/index.mdx"
          for FILE in "${FILES[@]}"; do
            [ -f "$FILE" ] || continue
            NAME=$(basename "$FILE" .mdx)
            echo "| $NAME | [Nextra 바로가기](/til/tags/$NAME) | [GitHub 바로가기](https://github.com/MinHyeok-lee1/TIL/blob/main/tags/$NAME.md) |" >> "$TAGS_DIR/index.mdx"
          done

          # til index.mdx
          echo "# 📓 Today I Learned (TIL)" > "$BASE/index.mdx"
          echo "> 꾸준한 기록은 꾸준한 성장으로 이어진다." >> "$BASE/index.mdx"
          echo "" >> "$BASE/index.mdx"
          echo "[🏷️ 태그모음](/til/tags)" >> "$BASE/index.mdx"
          echo "" >> "$BASE/index.mdx"
          echo "## 📅 연도별" >> "$BASE/index.mdx"

          # til-YYYY/_meta.json, til-YYYY/index.mdx
          for YEAR_DIR in "$BASE"/til-*; do
            [ -d "$YEAR_DIR" ] || continue
            YEAR=$(basename "$YEAR_DIR" | cut -d'-' -f2)
            echo ",  \"til-$YEAR\": \"${YEAR}년 TIL\"" >> "$BASE/_meta.json"

            echo "# 문서개요" > "$YEAR_DIR/index.mdx"
            echo "$YEAR년 TIL 기록입니다." >> "$YEAR_DIR/index.mdx"
            echo "| 월 | Nextra 바로가기 | GitHub 바로가기 |" >> "$YEAR_DIR/index.mdx"
            echo "|----|------------------|------------------|" >> "$YEAR_DIR/index.mdx"

            META="$YEAR_DIR/_meta.json"
            echo "{" > "$META"
            FIRST=1

            for MONTH_DIR in "$YEAR_DIR"/til-*; do
              [ -d "$MONTH_DIR" ] || continue
              MONTH=$(basename "$MONTH_DIR" | cut -d'-' -f2)
              if [ $FIRST -eq 1 ]; then
                echo "  \"til-$MONTH\": \"${MONTH}월 TIL\"" >> "$META"
                FIRST=0
              else
                echo "  ,\"til-$MONTH\": \"${MONTH}월 TIL\"" >> "$META"
              fi

              echo "- [$MONTH월 TIL](/til/til-$YEAR/til-$MONTH)" >> "$YEAR_DIR/index.mdx"

              # til-YYYY/til-MM/_meta.json, til-YYYY/til-MM/index.mdx
              MONTH_META="$MONTH_DIR/_meta.json"
              echo "{" > "$MONTH_META"
              FIRST_FILE=1
              FILES=( "$MONTH_DIR"/*.mdx )
              for FILE in "${FILES[@]}"; do
                [ -f "$FILE" ] || continue
                FNAME=$(basename "$FILE" .mdx)
                [ "$FNAME" = "index" ] && continue
                DAY=$(echo "$FNAME" | cut -d'-' -f1)
                TITLE_CLEAN=$(echo "$FNAME" | cut -d'-' -f2- | sed 's/-/ /g')
                if [ $FIRST_FILE -eq 1 ]; then
                  echo "  \"$FNAME\": \"${DAY}일 ${TITLE_CLEAN}\"" >> "$MONTH_META"
                  FIRST_FILE=0
                else
                  echo "  ,\"$FNAME\": \"${DAY}일 ${TITLE_CLEAN}\"" >> "$MONTH_META"
                fi
              done
              echo "}" >> "$MONTH_META"
            done
            echo "}" >> "$META"
          done
          echo "}" >> "$BASE/_meta.json"

      - name: Commit and Push
        run: |
          cd portfolio-repo
          git config user.name "MinHyeok-lee1"
          git config user.email "minhyeok.lee1@gmail.com"
          git add .
          if git diff --cached --quiet; then
            echo "✅ No changes to commit"
            exit 0
          fi
          git commit -m "📝 Sync TIL to portfolio: $(date +'%Y-%m-%d')"
          git push

      # - name: Deploy to Vercel
      #   if: always()
      #   run: |
      #     set -e
      #     npm install -g vercel
      #     FORCE_FLAG=""
      #     if [ "${{ steps.pushcheck.outputs.should_force }}" == "true" ]; then
      #       FORCE_FLAG="--force"
      #     fi
      #     vercel --token ${{ secrets.VERCEL_TOKEN }} --prod --yes $FORCE_FLAG --debug
      #   working-directory: portfolio-repo
